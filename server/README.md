# Exotic Telegram Server - Инструкция по развёртыванию

## Требования
- Аккаунт на [Cloudflare](https://cloudflare.com) (бесплатно)
- Node.js установлен локально
- Telegram аккаунт

## Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя бота, например: `Exotic Assistant Notifications`
4. Придумайте username, например: `ExoticAutoclickerBot`
5. **Сохраните токен** — он выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Настройка Cloudflare

### 2.1. Создайте аккаунт
Зарегистрируйтесь на [cloudflare.com](https://cloudflare.com) если ещё нет аккаунта.

### 2.2. Установите Wrangler CLI
```bash
npm install -g wrangler
```

### 2.3. Авторизуйтесь
```bash
wrangler login
```
Откроется браузер для авторизации.

### 2.4. Создайте KV Namespace
```bash
wrangler kv:namespace create USERS
```
Скопируйте полученный ID (например: `1234567890abcdef...`)

### 2.5. Обновите wrangler.toml
Откройте `server/wrangler.toml` и раскомментируйте/добавьте:
```toml
kv_namespaces = [
  { binding = "USERS", id = "ВАШ_KV_ID" }
]
```

## Шаг 3: Развёртывание

### 3.1. Перейдите в папку сервера
```bash
cd server
```

### 3.2. Добавьте токен бота как секрет
```bash
wrangler secret put TELEGRAM_BOT_TOKEN
```
Введите токен бота когда попросят.

### 3.3. Разверните Worker
```bash
wrangler deploy
```

Вы получите URL вида: `https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev`

## Шаг 4: Настройка Webhook

Настройте webhook для получения сообщений от бота. Выполните в браузере (замените значения):

```
https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/webhook
```

Вы должны увидеть: `{"ok":true,"result":true,"description":"Webhook was set"}`

## Шаг 5: Обновите расширение

В файлах `background.js` и `options.js` замените URL сервера:

```javascript
TELEGRAM_SERVER = 'https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev';
```

## Готово! 🎉

Теперь пользователи смогут:
1. Открыть настройки расширения
2. Нажать "Подключить" в разделе Telegram
3. Перейти по ссылке и нажать /start в боте
4. Получать уведомления о заявках

---

## Бесплатные лимиты Cloudflare Workers

- **100,000 запросов в день** (бесплатно)
- **KV хранилище**: 1 GB бесплатно
- **Время выполнения**: 10ms CPU time

Этого более чем достаточно для тысяч пользователей!

## Мониторинг

Вы можете видеть логи и статистику в [Cloudflare Dashboard](https://dash.cloudflare.com) → Workers → exotic-telegram → Logs

## Команды бота

- `/start` — Начать работу
- `/status` — Проверить статус подключения
- `/disconnect` — Отключить уведомления
- `/help` — Справка
