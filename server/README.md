# Exotic Telegram Server - Инструкция по развёртыванию

## Архитектура системы лицензирования

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Расширение    │────▶│  Cloudflare      │◀────│  Telegram Bot   │
│   (Chrome)      │     │  Worker + KV     │     │  (webhook)      │
└─────────────────┘     └──────────────────┘     └─────────────────┘
        │                       │                        │
        ▼                       ▼                        ▼
   oderId (UUID)          license:tg:{id}           telegramId
   deviceId               user:{oderId}             username
```

## Быстрый старт

```bash
cd server
npm install
npm run dev      # Локальная разработка
npm test         # Запуск тестов
npm run deploy   # Деплой в Cloudflare
```

## Настройка secrets (ОБЯЗАТЕЛЬНО)

```bash
wrangler secret put TELEGRAM_BOT_TOKEN      # Токен от @BotFather
wrangler secret put ADMIN_TOKEN             # Токен для REST API
wrangler secret put LOG_CHAT_ID             # Ваш Telegram ID для логов
wrangler secret put TELEGRAM_WEBHOOK_SECRET # Секрет для верификации webhook
wrangler secret put ADMIN_USERNAME          # Username админа (по умолчанию YaMob)
```

## Мониторинг

- **Health check**: `GET /api/health` — статус сервиса, uptime, метрики
- **Metrics**: `GET /api/metrics` (требует `Authorization: Bearer ADMIN_TOKEN`) — детальная статистика

### Структура данных в KV:
- `user:{oderId}` → данные связки расширение-telegram
- `tg:{telegramId}` → oderId (обратная связь)
- `license:tg:{telegramId}` → данные лицензии (основной ключ)
- `license:{username}` → копия лицензии (резервный поиск)
- `code:{code}` → временный код привязки (TTL 10 мин)
- `trial:{id}` → флаг использования пробного периода

### API Endpoints:
- `POST /api/license/check` - проверка лицензии
- `POST /api/generate-code` - генерация кода привязки
- `POST /api/status` - статус подключения
- `POST /api/notify` - отправка уведомления
- `POST /api/telegram/connect` - привязка (вызывается ботом)
- `POST /api/admin/grant-license` - выдача лицензии (админ)
- `POST /api/license/info` - информация о лицензии
- `POST /webhook` - Telegram webhook

### Telegram Bot команды:
- `/start` - приветствие + главное меню
- `/start CODE` - привязка расширения по коду
- `/status` - статус подписки
- `/help` - справка
- `/menu` - главное меню

### Inline-кнопки бота:
| Кнопка | Действие |
|--------|----------|
| 📊 Статус подписки | Показывает тип, срок, дни |
| 📖 Помощь | Инструкции по использованию |
| 🔗 Как подключить | Пошаговая инструкция |
| ⭐ Купить подписку | Тарифы и способы оплаты |
| 💬 Поддержка | Ссылка на @YaMob |
| ❓ FAQ | Частые вопросы |
| 🔄 Обновить | Обновить статус |
| ◀️ Главное меню | Вернуться в меню |

### Админ-команды (только для @YaMob):
| Команда | Описание |
|---------|----------|
| `/admin` | Открыть админ-панель |
| `/grant @username 30` | Выдать подписку на 30 дней |
| `/grant 123456789 7` | Выдать по Telegram ID |
| `/revoke @username` | Отозвать подписку |
| `/check @username` | Проверить пользователя |
| `/users` | Информация о списке |
| `/stats` | Статистика бота |

## Требования
- Аккаунт на [Cloudflare](https://cloudflare.com) (бесплатно)
- Node.js установлен локально
- Telegram аккаунт

## Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя бота, например: `Exotic Assistant Notifications`
4. Придумайте username, например: `clickuved_bot`
5. **Сохраните токен** — он выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Настройка Cloudflare

### 2.1. Создайте аккаунт
Зарегистрируйтесь на [cloudflare.com](https://cloudflare.com) если ещё нет аккаунта.

### 2.2. Установите Wrangler CLI
```bash
npm install -g wrangler
```

### 2.3. Авторизуйтесь
```bash
wrangler login
```
Откроется браузер для авторизации.

### 2.4. Создайте KV Namespace
```bash
wrangler kv:namespace create USERS
```
Скопируйте полученный ID (например: `1234567890abcdef...`)

### 2.5. Обновите wrangler.toml
Откройте `server/wrangler.toml` и раскомментируйте/добавьте:
```toml
kv_namespaces = [
  { binding = "USERS", id = "ВАШ_KV_ID" }
]
```

## Шаг 3: Развёртывание

### 3.1. Перейдите в папку сервера
```bash
cd server
```

### 3.2. Добавьте секреты
```bash
# Токен бота
wrangler secret put TELEGRAM_BOT_TOKEN

# Админский токен для выдачи лицензий (придумайте сложный)
wrangler secret put ADMIN_TOKEN
```
Введите значения когда попросят.

### 3.3. Разверните Worker
```bash
wrangler deploy
```

Вы получите URL вида: `https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev`

## Шаг 4: Настройка Webhook

### Автоматическая установка (рекомендуется):
Откройте в браузере:
```
https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/api/set-webhook
```
Вы должны увидеть: `{"ok":true,"result":true,"webhookUrl":"..."}`

### Ручная установка:
```
https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/webhook
```

### Проверка webhook:
```
https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/api/webhook-info
```

### Проверка здоровья сервера:
```
https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/api/health
```

## Шаг 5: Обновите расширение

В файлах `background.js` и `options.js` замените URL сервера:

```javascript
TELEGRAM_SERVER = 'https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev';
```

## Управление лицензиями

### Выдача лицензии через API:
```bash
curl -X POST https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/api/admin/grant-license \
  -H "Authorization: Bearer ВАШ_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"telegramId": "123456789", "days": 30, "type": "subscription"}'
```

### Проверка лицензии:
```bash
curl -X POST https://exotic-telegram.YOUR_SUBDOMAIN.workers.dev/api/license/info \
  -H "Content-Type: application/json" \
  -d '{"telegramId": "123456789"}'
```

### Типы лицензий:
- `trial` - пробный период (выдаётся автоматически, 24 часа)
- `subscription` - обычная подписка

## Troubleshooting

### Лицензия не видна:
1. Проверьте что Telegram привязан (`/api/status`)
2. Проверьте что лицензия выдана (`/api/license/info`)
3. Проверьте дату истечения

### Бот не отвечает:
1. Проверьте webhook: `https://api.telegram.org/bot<TOKEN>/getWebhookInfo`
2. Проверьте логи в Cloudflare Dashboard

### Ошибка "NOT_CONNECTED":
Пользователь не привязал Telegram. Нужно:
1. Нажать "Подключить" в настройках расширения
2. Перейти по ссылке в Telegram
3. Отправить /start боту

## Готово! 🎉

Теперь пользователи смогут:
1. Открыть настройки расширения
2. Нажать "Подключить" в разделе Telegram
3. Перейти по ссылке и нажать /start в боте
4. Получать уведомления о заявках

---

## Бесплатные лимиты Cloudflare Workers

- **100,000 запросов в день** (бесплатно)
- **KV хранилище**: 1 GB бесплатно
- **Время выполнения**: 10ms CPU time

Этого более чем достаточно для тысяч пользователей!

## Мониторинг

Вы можете видеть логи и статистику в [Cloudflare Dashboard](https://dash.cloudflare.com) → Workers → exotic-telegram → Logs

## Команды бота

- `/start` — Начать работу
- `/status` — Проверить статус подключения
- `/disconnect` — Отключить уведомления
- `/help` — Справка
